<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.camp.app.member.mapper.MemberMapper">
	<select id="findAll" resultType="MemberVO">
		SELECT * FROM member
	</select>
	<select id="findById" parameterType="Integer" resultType="MemberVO">
		SELECT * FROM member WHERE member_id = #{memberId}
	</select>
	<select id="findByEmail" parameterType="String" resultType="MemberVO">
		SELECT * FROM member WHERE member_email = #{memberEmail}
	</select>
	<insert id="save" parameterType="MemberVO">
		<selectKey resultType="Integer" keyProperty="memberId" order="BEFORE" >
			SELECT MAX(member_id)+1 FROM board
		</selectKey>
		INSERT
	</insert>
	<select id="count" resultType="Integer">
		SELECT COUNT(*) FROM member
	</select>
</mapper>